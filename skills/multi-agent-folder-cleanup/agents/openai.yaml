interface:
  display_name: "Multi-Agent Folder Cleanup"
  short_description: "Audit project folders and portfolios safely"
  default_prompt: "Use $multi-agent-folder-cleanup to audit this shared project folder or portfolio and explain what is authoritative, stale, operationally verified, incoming, or ambiguous."
policy:
  allow_implicit_invocation: true
